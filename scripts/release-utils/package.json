{
  "name": "@eyeo/release-utils",
  "type": "module",
  "scripts": {
    "do-release": "node do-release.js",
    "test": "vitest run --exclude test/end-to-end --reporter=verbose",
    "test:end-to-end": "vitest run --root test/end-to-end --reporter=verbose"
  },
  "devDependencies": {
    "yargs": "^17.7.2",
    "vitest": "^2.0.5"
  }
}
