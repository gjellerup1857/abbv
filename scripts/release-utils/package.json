{
  "name": "@eyeo/release-utils",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "do-release": "node do-release.js"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
