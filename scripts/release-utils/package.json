{
  "name": "@eyeo/release-utils",
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "version-bump": "node version-bump.js"
  },
  "devDependencies": {
    "vitest": "^2.0.5"
  }
}
