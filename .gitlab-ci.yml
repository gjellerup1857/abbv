default:
  tags:
    - "GCP"

include:
  - local: 'host/adblock/.gitlab-ci.yml'
    inputs:
      hostpath: "host/adblock"
  - local: 'host/adblockplus/.gitlab-ci.yml'
    inputs:
      hostpath: "host/adblockplus"
