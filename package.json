{
  "name": "extensions",
  "private": true,
  "description": "Extensions monorepo",
  "workspaces": [
    "host/*",
    "fragment/*",
    "core/*",
    "ui-components"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
