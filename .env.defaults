# These can either be filled in with a .env file with the same format or set as
# environment variables. They are set in the CI environment using Gitlab
# secrets.

ADBLOCK_CDP_PING_URL=""
ADBLOCK_CDP_AGGREGATE_URL=""
ADBLOCK_CDP_BEARER=""
